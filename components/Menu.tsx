import styled from "styled-components";

const Menu = styled.div`
  position: absolute;
  width: 50%;
  max-height: 90vh;
  overflow-y: auto;
  box-shadow: 0 1px 3px 0 hsla(0, 0%, 0%, 0.2);
`;

export default Menu;
